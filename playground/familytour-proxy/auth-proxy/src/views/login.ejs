<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Sign in</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg: #0b1020;
            --card: #12182b;
            --text: #e8eefc;
            --muted: #a6b0cf;
            --accent: #6ba4ff;
            --accent2: #9f7cff;
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: 'Inter', system-ui, -apple-system, Segoe UI, Roboto, Arial;
            background: radial-gradient(1200px 800px at 10% 10%, #0e1630, #080d1a 70%), var(--bg);
            color: var(--text);
        }

        .grid {
            min-height: 100vh;
            display: grid;
            place-items: center;
            padding: 24px;
        }

        .card {
            width: 100%;
            max-width: 420px;
            background: linear-gradient(180deg, rgba(255, 255, 255, 0.04), rgba(255, 255, 255, 0.02));
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 16px;
            padding: 28px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.4);
            position: relative;
            overflow: hidden;
        }

        .card:before {
            content: "";
            position: absolute;
            inset: -2px;
            background: radial-gradient(600px 200px at -10% -10%, rgba(107, 164, 255, 0.2), transparent 60%), radial-gradient(600px 200px at 110% 10%, rgba(159, 124, 255, 0.18), transparent 60%);
            filter: blur(20px);
            z-index: 0;
        }

        h1 {
            margin: 0 0 12px;
            font-size: 26px;
            letter-spacing: .2px;
            position: relative;
            z-index: 1;
        }

        p.muted {
            margin: 0 0 18px;
            color: var(--muted);
            font-size: 14px;
            position: relative;
            z-index: 1;
        }

        form {
            display: grid;
            gap: 14px;
            position: relative;
            z-index: 1;
        }

        label {
            font-size: 13px;
            color: var(--muted);
            margin-bottom: 6px;
            display: block;
        }

        input {
            width: 100%;
            padding: 12px 14px;
            background: #0e1427;
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 10px;
            color: var(--text);
            outline: none;
            transition: border-color .2s, box-shadow .2s;
        }

        input:focus {
            border-color: var(--accent);
            box-shadow: 0 0 0 3px rgba(107, 164, 255, 0.2);
        }

        .btn {
            display: inline-flex;
            justify-content: center;
            align-items: center;
            gap: 8px;
            padding: 12px 16px;
            border-radius: 10px;
            border: 1px solid transparent;
            background: linear-gradient(135deg, var(--accent), var(--accent2));
            color: white;
            font-weight: 600;
            cursor: pointer;
            transition: transform .05s ease-in-out, box-shadow .2s;
            box-shadow: 0 6px 22px rgba(107, 164, 255, 0.25);
        }

        .btn:hover {
            transform: translateY(-1px);
            box-shadow: 0 8px 28px rgba(107, 164, 255, 0.35);
        }

        .row {
            display: grid;
            gap: 6px;
        }

        .error {
            background: rgba(255, 77, 109, 0.1);
            border: 1px solid rgba(255, 77, 109, 0.3);
            color: #ff8aa1;
            padding: 10px 12px;
            border-radius: 10px;
            font-size: 13px;
            margin-bottom: 14px;
        }

        .fineprint {
            margin-top: 14px;
            color: var(--muted);
            font-size: 12px;
            text-align: center;
        }
    </style>
</head>

<body>
    <div class="grid">
        <div class="card">
            <h1>Welcome</h1>
            <p class="muted">Sign in to continue to Open WebUI</p>

            <% if (error) { %>
                <div class="error">
                    <%= error %>
                </div>
            <% } %>

            <% if (!hideForm) { %>
                <form method="post" action="/login">
                    <div class="row">
                        <label for="username">Name</label>
                        <input id="username" name="username" type="text" placeholder="arsXXXX" required autofocus />
                    </div>
                    <div class="row">
                        <label for="password">Password</label>
                        <input id="password" name="password" type="password" placeholder="XXXXXX" required />
                    </div>
                    <button class="btn" type="submit">Sign in</button>
                </form>

                <div class="fineprint">Sign in with your assigned Name and Password from the list.</div>
            <% } %>
        </div>
    </div>
</body>

</html>
